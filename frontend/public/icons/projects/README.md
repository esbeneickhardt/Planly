# Project icons

Drop icon images here (PNG, SVG, WebP) to make them available as selectable project/product icons in the UI.

Files placed here are served at `/icons/projects/<filename>` with no build step required.

Suggested size: 64×64 px minimum, square aspect ratio.
