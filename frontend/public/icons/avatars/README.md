# Avatar icons

Drop avatar images here (PNG, SVG, WebP) to make them available as selectable user avatars in the UI.

Files placed here are served at `/icons/avatars/<filename>` with no build step required.

Suggested size: 64×64 px minimum, square aspect ratio.
